# Supabase Connection Configuration
# =================================
# Copy this file to secrets.toml and fill in your actual Supabase credentials
# DO NOT commit secrets.toml to git (it's in .gitignore)

# Option 1: Connection String (Recommended)
# Get this from Supabase Dashboard → Settings → Database → Connection string
[connections.supabase]
SUPABASE_DB_URL = "postgresql://postgres.xxxxx:your-password@db.xxxxx.supabase.co:5432/postgres"

# Option 2: Individual Parameters (Alternative)
# Uncomment and fill these if you prefer individual parameters
# [connections.supabase]
# SUPABASE_DB_HOST = "db.xxxxx.supabase.co"
# SUPABASE_DB_PORT = "5432"  # Use "6543" for connection pooler (recommended for production)
# SUPABASE_DB_NAME = "postgres"
# SUPABASE_DB_USER = "postgres.xxxxx"
# SUPABASE_DB_PASSWORD = "your-password"

# Notes:
# - For local development: Create .streamlit/secrets.toml with your credentials
# - For Streamlit Cloud: Add these secrets in your app's settings (Edit Secrets)
# - Connection pooler (port 6543) is recommended for better performance and connection management
# - Find your connection details in Supabase Dashboard → Settings → Database

